rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules allow full read/write access for DEVELOPMENT ONLY.
    // Lock this file down before deploying to production traffic.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}